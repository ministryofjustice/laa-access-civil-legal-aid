# Default values for laa-access-civil-legal-aid in the dnstest environment.
# The laa-access-civil-legal-aid-dnstest namespace installs a new helm release per feature branch
# this is then uninstalled when the branch is merged.
environment: "dnstest"

ingress:
  enabled: true
  tls:
    - hosts:
        - "access-civil-legal-aid-dnstest.cloud-platform.service.justice.gov.uk"
  hosts:
    - host: "access-civil-legal-aid-dnstest.cloud-platform.service.justice.gov.uk"
      paths:
        - path: /
          pathType: ImplementationSpecific

envVars:
  DEBUG:
    value: "True"
  CLA_BACKEND_URL:
    value: "http://cla-backend-app.laa-cla-backend-uat.svc.cluster.local"